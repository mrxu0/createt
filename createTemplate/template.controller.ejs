import {
  Controller as controller,
  Get as get,
  Query as query,
} from '@nestjs/common';
import { <%= firstUpperCase(name) %>Service } from './<%= name %>.service';

@controller('<%= name %>')
/** 匿名 controller */
export class <%= firstUpperCase(name) %>Controller {
  /** 构造函数 */
  constructor(private readonly <%= name %>Service: <%= firstUpperCase(name) %>Service) {}

  /** 创建匿名 */
  @get('create')
  create(@query('content') content) {
    return this.<%= name %>Service.create(content);
  }
}
