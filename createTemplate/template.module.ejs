import { Module } from '@nestjs/common';
import { TypeOrmModule } from '@nestjs/typeorm';
import { <%= firstUpperCase(name) %>Service } from './<%= name %>.service';
import { <%= firstUpperCase(name) %>Controller } from './<%= name %>.controller';
import { <%= firstUpperCase(name) %> } from './entities/<%= name %>.entity';

@Module({
  imports: [TypeOrmModule.forFeature([<%= firstUpperCase(name) %>])],
  providers: [<%= firstUpperCase(name) %>Service],
  controllers: [<%= firstUpperCase(name) %>Controller],
})
/** 钉钉相关的定时任务 */
export class <%= firstUpperCase(name) %>Module {}
