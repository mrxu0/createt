import { Module } from '@nestjs/common';
import { TypeOrmModule } from '@nestjs/typeorm';
import { <%= name.toUpperCase() %>Service } from './<%= name %>.service';
import { <%= name.toUpperCase() %>Controller } from './<%= name %>.controller';
import { <%= name.toUpperCase() %> } from './entities/<%= name %>.entity';

@Module({
  imports: [TypeOrmModule.forFeature([<%= name.toUpperCase() %>])],
  providers: [<%= name.toUpperCase() %>Service],
  controllers: [<%= name.toUpperCase() %>Controller],
})
/** 钉钉相关的定时任务 */
export class <%= name.toUpperCase() %>Module {}
